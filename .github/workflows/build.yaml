name: Build

on:
  workflow_dispatch:
  push: 
    branches:
      - dev
    paths:
      - 'src/**'
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: m365pnp/spfx:1.14.0
    steps:
      - run: npm install
      - run: gulp bundle --ship
      - run: ls
